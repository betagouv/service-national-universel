{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".build/**", "!.build/cache/**"]
    },
    "start": {
      "outputs": [".launch/**", "!.launch/cache/**"]
    },
    "dev": {
      "persistent": true
    }
  }
}
