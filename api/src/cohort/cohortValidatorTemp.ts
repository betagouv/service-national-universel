// Function will be updated in the next PR, for the time being it's useful to test typing import from snu-lib

import { CohortDto } from "snu-lib/src/dto";

const validator = (dto: CohortDto) => {
  const bla = dto.busEditionOpenForTransporter;
};
