export const syncAppelAProjet = () => {
  // get appel à projet from dsprovider => {classe, etablissement}
  // for each etablissement,
  // --> get complementary data from annuaireEtablissementProvider
  // --> if etablissement is does not exist
  // ----> call etablissementService.create
  // --> else if etablissement exists
  // ----> call etablissementService.update
  // create referent
};
