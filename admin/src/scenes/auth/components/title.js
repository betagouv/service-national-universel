import styled from "styled-components";

const Title = styled.h1`
  position: relative;
  font-size: 2rem;
  @media (max-width: 768px) {
    font-size: 1.2rem;
  }
  font-weight: 700;
  margin-bottom: 14px;
`;

export default Title;
