import React from "react";
import styled from "styled-components";

export default () => <Container>Merci</Container>;

const Container = styled.div`
  display: flex;
  flex: 1;
  justify-content: center;
  align-items: center;
  font-size: 3rem;
`;
