import styled from "styled-components";

export default styled.div`
  font-weight: 500;
  font-size: 0.8rem;
  text-align: center;
  margin-top: 0.5rem;
  :hover {
    text-decoration: underline;
    cursor: pointer;
  }
`;
